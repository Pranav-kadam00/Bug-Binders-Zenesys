# Local development — backend running on the default port.
# The Vite dev server proxies /api/* to this origin, so the browser
# never makes cross-origin requests during development.
VITE_API_BASE_URL=http://localhost:8000
